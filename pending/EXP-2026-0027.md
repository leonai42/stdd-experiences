---
category: contract_gap
confidence: 0.75
detection_trigger: review attribution 测试断言失败：assert "分配效应" in result.output 报
  exit_code 0 但断言不命中。逐字符比对后发现实现输出 "配置效应"（配=0x914d,置=0x7f6e），
  而测试/spec 断言 "分配效应"（分=0x5206,配=0x914d）——不是编码问题，是中文术语漂移。
experience_id: EXP-2026-0027
first_seen: '2026-08-25'
fix_template: '1. 中文术语（尤其财务/组合术语，如 配置效应 vs 分配效应）易在实现时换词，造成
  与已批准 spec 的契约断层 → 断言不命中。

  2. Canonical-First：以 spec 中 THEN 的原文为准对齐代码输出，而非反向改测试（spec 是权威）。

  3. 中文断言失败时先做逐字符/码点比对（ascii()/hex(ord(ch))），排除"以为是编码问题"的误判；
  GBK 终端下 UTF-8 源码与 GBK 解码输出都显示为乱码，容易把术语漂移误判为编码问题。

  4. 对齐后重跑该断言确认命中，并跑全量回归确认无其他术语漂移。
  '
language: python
last_seen: '2026-08-25'
lifecycle_state: discovered
occurrences: 1
pattern: CLI 输出中文术语与已批准 spec 不一致 → 测试断言不命中（易被误判为编码问题）
project_type: python
provenance: ai-inferred
provenance_weight: 0.6
root_cause: 实现阶段对同一概念换了近义词（配置 vs 分配），未逐字对齐 spec 的 THEN 原文；
  中文在 GBK 终端下显示乱码，掩盖了真实差异。
severity: medium
source_change: 2026-08-24-leftover-fixes
source_file: daostock<project>/<module>
title: 中文术语漂移致断言不命中 — 代码输出须对齐 spec 原文
---

# EXP-2026-0027 中文术语漂移致断言不命中

C07 S2 attribution 测试 `assert "分配效应" in result.output` 断言不命中（exit_code 0）。
逐字符比对发现实现输出 "配置效应"，与已批准 spec THEN 的 "分配效应" 不一致。Canonical-First：
以 spec 原文为准对齐代码输出。详见 frontmatter 的 pattern / root_cause / fix_template。

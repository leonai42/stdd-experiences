---
category: coverage_vacuum
confidence: 0.9
detection_trigger: pytest --cov=daostock.X 多模块测量时，numpy 聚合测试（std/var/sum/chip分布/缠论/screen 因子）集体失败，报 `int() ... not '_NoValueType'`；无覆盖全量回归（pytest tests/）全绿
experience_id: EXP-2026-0026
first_seen: '2026-08-24'
fix_template: |
  1. 以无覆盖全量回归 `pytest tests/` 为权威质量门；覆盖率测量单独进行，不受假红干扰。
  2. 覆盖测量报数时，若测量运行含聚合测试假红，报告中必须标注"测量受 numpy 双加载缺陷影响，非代码回归"，并附无覆盖复跑证据。
  3. 不要在 conftest 预导入 numpy/pandas 规避——实测无效（coverage 插桩 finder 会绕过 sys.modules 缓存对 numpy 子模块二次加载）。
  4. 判定回归前，先在无覆盖模式复跑失败用例；全绿即工具缺陷，不是代码缺陷。
  5. 新增 numpy 聚合相关测试后，须同时验证无覆盖运行通过，避免把假红误记为回归。
language: python
last_seen: '2026-08-24'
lifecycle_state: discovered
occurrences: 1
pattern: 多模块 --cov 测量触发 numpy 二次加载 → numpy 聚合测试集体假红
project_type: python
provenance: ai-inferred
provenance_weight: 0.6
root_cause: |
  coverage source 插桩 + numpy 在 coverage 启动后才导入 → numpy 二次加载。
  pandas 检测到 "The NumPy module was reloaded"；二次加载 numpy 实例的 _NoValue 哨兵
  与 umr_sum 内部识别的哨兵身份不一致，numpy/_core/_methods._sum 在掩码聚合
  （mask.sum → nanvar/_get_counts）路径把哨兵当真实值传入 → `int(_NoValue)` TypeError。
  触发面是覆盖测量本身（pytest-cov 或 coverage run 一致复现），无覆盖时全部通过。
  在 C06 表现为 18 例 numpy 聚合测试失败、332 通过；同一代码无覆盖 350 全绿。
severity: medium
source_change: 2026-08-24-market-wiring
source_file: tests/（覆盖率测量基础设施）
tags:
  - coverage
  - numpy
  - test-infra
  - false-positive
---

---
adoption_count: 0
category: contract_gap
community_votes_unuseful: 0
community_votes_useful: 0
confidence: 0.5
detection_trigger: 消费者报空/零/中性提示而不报错；仓里既有同类文件用的是另一个键名；grep 该键名只出现在注释与本次新增文件里
experience_id: EXP-b394e235f737
first_seen: '2026-09-11'
fix_template: 1) 以**消费侧代码**为唯一事实源确定键名（注释不算）；2) 产出侧改为该键，其余内容逐字保留；3) 加断言：声明条数 == 消费者实际取到的条数（两侧同源即为恒真，须让声明侧同时接受两种键，否则键一改两边同减、断言恒真）；4)
  空输入必须报「查过哪些位置、期望哪个键」而非一个中性词
language: python
last_seen: '2026-09-11'
lifecycle_state: discovered
occurrences: 1
pattern: 规格文件内容写对了但用了消费者不读的键：消费者恒得空输入且静默（no-specs / 恒 SKIP / 恒 0），既不报错也不提示查过哪些位置
project_type: toolchain
provenance: ai-inferred
provenance_weight: 0.6
root_cause: 产出侧按注释或直觉选键名，消费侧读的是另一个键；两侧之间没有可执行的一致性断言，错注释还会主动把产出侧带偏
severity: high
source_change: 2026-09-11-v391-friction-repair
source_file: ''
tags:
- spec
- consumer
- key-mismatch
- silent-empty
- verify

audit:
  audited_on: '2026-09-25'
  audit_commit: 142542c
  verdict: approve-generalized
  original_confidence: 0.5      # 导出管道默认值，非作者评估
  assigned_confidence: 0.85
  assigned_severity: high
  generalized_to: EXP-JI-0011
  target_pack: judgment-integrity
  note: 'B 桶（通用化后入池）：已去除 STDD 工具链内部标识与项目专有名词后落包'
---

本 change 现场：`stdd verify run --adapter local-command` 对 change 自身
恒报 `no-specs` → `phases.build.verified_by` 写不出 → Gate 3 阻塞。
此时 S1–S9 全部完成、全量 991 全绿、`ci check-failures` 0 个 ❌。

根因链三段，每段单独看都「对」：`verify.py`/`agent.py` 读 `steps`；
但 `agent.py` 与 `assertions.py` 两处**注释**写着消费者读 `contract_points`；
Phase 2 照注释写出 9 个用 `contract_points` 的 spec —— 内容全对，对消费者等于不存在。

**九轮自查（含 5 组变异探针）都在查「我写的代码对不对」，没有一轮在查
「我写的东西有没有人读」。** 见 ADJ-027。
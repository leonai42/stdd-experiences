---
adoption_count: 0
category: tool_misuse
community_votes_unuseful: 0
community_votes_useful: 0
confidence: 0.5
detection_trigger: CI 项 (o) 报 canonical 冻结偏离，但：该文件 mtime 早于冻结时刻；对现字节穷举读法/编码/换行/YAML
  往返变体均无法复现记录值；同一批冻结文件里「在 Gate 1 之前写入的」相符、「在 Gate 1 之后写入的」不符。最后那个不对称就是指纹 —— 它把「文件被改过」排除掉
experience_id: EXP-e4e124c1f186
first_seen: '2026-09-22'
fix_template: 判据不是「文件有没有被改」，而是「记录值指向的字节是不是一份真实交付物」。三步：(1) 列全部冻结文件的 mtime 与各 Gate 时刻，找「早于/晚于」的不对称；(2)
  对现字节穷举读法变体，确认无法复现记录值（若某个变体能复现，则是记录端与校验端读法不一致，另一种缺陷）；(3) 读冻结写入方源码确认 setdefault 语义。归因到机制后按登记通道逐文件字面全路径放行，并把机制链留痕供修工具。修法：Gate
  2 应对「自 Gate 1 后在 specs 段新增或实质改写的文件」重算基线
language: python
last_seen: '2026-09-22'
lifecycle_state: discovered
occurrences: 1
pattern: 冻结基线停在脚手架占位壳上：Gate 1 本身就冻结 specs 段（DOC_FREEZE_SECTIONS 含 specs/code 与 specs/agent），而这两份
  spec YAML 是建 change 时由 canon init 用模板写下的占位壳；冻结写入方用 setdefault（首次记录为准、此后永不重算），故 Gate
  2 不会刷新已被 Gate 1 登记的文件 —— 于是在 Gate 1 之后才写入真内容的 spec 永久显示为「冻结偏离」，CI 项 (o) 报「已修改但未登记」，而交付本身逐字未动
project_type: null
provenance: ai-inferred
provenance_weight: 0.6
root_cause: 冻结规则自身冲突：把「Phase 2 才写内容的文件」纳入 Gate 1 的冻结范围，同时又用 setdefault 禁止 Gate 2 重算已登记文件。两者叠加
  → 基线永久冻结在占位壳上，且 recorded_at 会随 Gate 2 更新而掩盖真实记录时刻
severity: high
source_change: 2026-09-21-cross-partial-fill-topup-and-peak-binding
source_file: ''
tags:
- freeze
- hash
- gate
- cli
- false-positive
audit:
  audited_on: '2026-09-26'
  audit_commit: 62b73cf
  verdict: out-of-scope
  bucket: D_建议移出社区池
  reason: '描述的是 STDD 工具链自身实现，下游社区用户无法据此行动'
  note: '非质量拒绝 —— 见解对工具维护者可能有用，故移入 out-of-scope/ 而非 rejected/'
  # 本批 8 条经逐条核对确认通篇为 STDD 工具链内部机制（Gate/change/canon/verify 适配器）；
  # EXP-20e2b6b23fe8 是唯一可争议的一条：它讲的是「子代理的验证结论必须落盘为交付物」，
  # 内核可迁移到任何多代理流程，但记录通篇用 C1/C7/phase-context 的工具链词汇写就，
  # 按 CONTRIBUTING「下游社区用户能否据此行动」判为移出。若日后增设「方法论」主题包，
  # 应复议此条。

---


